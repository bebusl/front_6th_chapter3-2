describe('반복 일정 기능', () => {
  describe('반복 일정 생성', () => {
    it('매일 반복 일정을 생성할 수 있다', () => {
      // Given: 사용자가 매일 반복 유형을 선택하고 일정 정보를 입력한다
      // When: 반복 일정 생성 버튼을 클릭한다
      // Then: 지정된 기간 동안 매일 반복되는 일정이 생성되고 캘린더에 반복 아이콘과 함께 표시된다
    });

    it('매주 반복 일정을 생성할 수 있다', () => {
      // Given: 사용자가 매주 반복 유형을 선택하고 일정 정보를 입력한다
      // When: 반복 일정 생성 버튼을 클릭한다
      // Then: 지정된 기간 동안 매주 반복되는 일정이 생성되고 캘린더에 반복 아이콘과 함께 표시된다
    });

    it('매월 반복 일정을 생성할 수 있다', () => {
      // Given: 사용자가 매월 반복 유형을 선택하고 일정 정보를 입력한다
      // When: 반복 일정 생성 버튼을 클릭한다
      // Then: 지정된 기간 동안 매월 반복되는 일정이 생성되고 캘린더에 반복 아이콘과 함께 표시된다
    });

    it('매년 반복 일정을 생성할 수 있다', () => {
      // Given: 사용자가 매년 반복 유형을 선택하고 일정 정보를 입력한다
      // When: 반복 일정 생성 버튼을 클릭한다
      // Then: 지정된 기간 동안 매년 반복되는 일정이 생성되고 캘린더에 반복 아이콘과 함께 표시된다
    });

    it('31일에 매월 반복 일정을 선택하면 31일이 있는 달에만 생성된다', () => {
      // Given: 사용자가 31일에 매월 반복 유형을 선택하고 일정 정보를 입력한다
      // When: 반복 일정 생성 버튼을 클릭한다
      // Then: 31일이 있는 달에만 반복 일정이 생성되고, 31일이 없는 달에는 생성되지 않는다
    });

    it('윤년 2월 29일에 매년 반복 일정을 선택하면 윤년에만 생성된다', () => {
      // Given: 사용자가 윤년 2월 29일에 매년 반복 유형을 선택하고 일정 정보를 입력한다
      // When: 반복 일정 생성 버튼을 클릭한다
      // Then: 윤년 2월 29일에만 반복 일정이 생성되고, 평년에는 생성되지 않는다
    });

    it('반복 종료일을 2025-10-31로 설정하면 해당 날짜까지만 반복된다', () => {
      // Given: 사용자가 반복 일정을 생성하고 종료일을 2025-10-31로 설정한다
      // When: 반복 일정 생성 버튼을 클릭한다
      // Then: 2025-10-31까지만 반복 일정이 생성되고 그 이후에는 생성되지 않는다
    });

    it('반복 일정 생성 즉시 캘린더와 리스트뷰에 모든 반복 일정이 표시된다', () => {
      // Given: 사용자가 반복 일정 정보를 입력한다
      // When: 반복 일정 생성 버튼을 클릭한다
      // Then: 캘린더뷰와 리스트뷰에 모든 반복 일정이 반복 아이콘과 함께 즉시 표시된다
    });
  });

  describe('반복 일정 표시', () => {
    it('반복 일정은 캘린더에 반복 아이콘으로 구분되어 표시된다', () => {
      // Given: 반복 일정이 생성되어 있다
      // When: 캘린더를 조회한다
      // Then: 반복 일정은 일반 일정과 구분되는 반복 아이콘과 함께 표시된다
    });

    it('반복 일정은 리스트뷰에서도 반복 아이콘으로 구분되어 표시된다', () => {
      // Given: 반복 일정이 생성되어 있다
      // When: 리스트뷰를 조회한다
      // Then: 반복 일정은 일반 일정과 구분되는 반복 아이콘과 함께 표시된다
    });
  });

  describe('반복 일정 단일 수정', () => {
    it('반복 일정 중 하나를 수정하면 해당 일정만 단일 일정으로 변경된다', () => {
      // Given: 반복 일정이 여러 개 생성되어 있다
      // When: 그 중 하나의 일정을 선택하여 수정한다
      // Then: 수정된 일정은 단일 일정으로 변경되고 반복 아이콘이 사라진다
    });

    it('반복 일정 중 하나를 수정해도 다른 반복 일정들은 영향받지 않는다', () => {
      // Given: 반복 일정이 여러 개 생성되어 있다
      // When: 그 중 하나의 일정을 선택하여 수정한다
      // Then: 다른 반복 일정들은 그대로 유지되고 반복 아이콘도 그대로 표시된다
    });

    it('반복 일정 수정 시 리스트뷰에서 수정 버튼을 누르면 왼쪽이 수정 폼으로 변환된다', () => {
      // Given: 반복 일정이 리스트뷰에 표시되어 있다
      // When: 해당 일정의 수정 버튼을 클릭한다
      // Then: 왼쪽 영역이 수정 폼으로 변환되고 선택한 일정의 정보가 폼에 세팅된다
    });

    it('단일 수정된 일정은 캘린더뷰와 리스트뷰에 즉시 반영된다', () => {
      // Given: 반복 일정 중 하나를 수정한다
      // When: 수정 내용을 저장한다
      // Then: 수정된 내용이 캘린더뷰와 리스트뷰에 즉시 반영된다
    });
  });

  describe('반복 일정 단일 삭제', () => {
    test('반복 일정 중 하나를 삭제하면 해당 일정만 삭제된다', () => {
      // Given: 반복 일정이 여러 개 생성되어 있다
      // When: 그 중 하나의 일정을 선택하여 삭제한다
      // Then: 선택한 일정만 삭제되고 다른 반복 일정들은 그대로 유지된다
    });

    test('반복 일정 중 하나를 삭제해도 다른 반복 일정들은 영향받지 않는다', () => {
      // Given: 반복 일정이 여러 개 생성되어 있다
      // When: 그 중 하나의 일정을 선택하여 삭제한다
      // Then: 다른 반복 일정들은 그대로 유지되고 반복 아이콘도 그대로 표시된다
    });

    test('단일 삭제된 일정은 캘린더뷰와 리스트뷰에 즉시 반영된다', () => {
      // Given: 반복 일정 중 하나를 삭제한다
      // When: 삭제를 확인한다
      // Then: 삭제된 일정이 캘린더뷰와 리스트뷰에서 즉시 사라진다
    });
  });
});
